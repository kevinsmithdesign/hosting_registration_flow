import React from "react";
import { useNavigate, Navigate } from "react-router-dom";
import { Button } from "@mui/material";

export default function ContinueButton() {
  const navigate = useNavigate();

  return <div>ContinueButton</div>;
}
